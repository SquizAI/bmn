import { clsx, type ClassValue } from 'clsx';

export function cn(...inputs: ClassValue[]) {
  return clsx(inputs);
}

export const APP_URL = 'https://app.brandmenow.com';
export const SITE_URL = 'https://brandmenow.com';
