# ============================================================
# Brand Me Now v2 -- Environment Variables Template
# Copy to .env and fill in values: cp .env.example .env
# ============================================================

# === App Config ===
NODE_ENV=development
PORT=4847
VITE_PORT=4848
MARKETING_PORT=4849
API_URL=http://localhost:4847
APP_URL=http://localhost:4848
MARKETING_URL=http://localhost:4849

# === Supabase ===
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# === Redis (local Docker: 6381, production: managed) ===
REDIS_URL=redis://localhost:6381

# === AI Providers ===
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GOOGLE_API_KEY=
# Production uses direct BFL API for FLUX.2 Pro (not Fal.ai proxy)
BFL_API_KEY=
IDEOGRAM_API_KEY=

# === Payments (Stripe test keys for dev) ===
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=
# Stripe Price IDs -- create products in Stripe Dashboard, then paste Price IDs here
STRIPE_PRICE_STARTER=price_starter_monthly
STRIPE_PRICE_PRO=price_pro_monthly
STRIPE_PRICE_AGENCY=price_agency_monthly

# === CRM (GoHighLevel OAuth 2.0) ===
GHL_CLIENT_ID=
GHL_CLIENT_SECRET=
GHL_REDIRECT_URI=http://localhost:4847/api/v1/auth/ghl/callback
GHL_LOCATION_ID=
# Optional: seed tokens from a previous OAuth flow (auto-refreshed at runtime)
# GHL_ACCESS_TOKEN=
# GHL_REFRESH_TOKEN=

# === Email ===
RESEND_API_KEY=
FROM_EMAIL=hello@brandmenow.com
SUPPORT_EMAIL=support@brandmenow.com

# === Scraping ===
APIFY_API_TOKEN=

# === Observability ===
SENTRY_DSN=
POSTHOG_API_KEY=
POSTHOG_HOST=https://us.posthog.com

# === Security ===
# Generate with: openssl rand -hex 32
RESUME_TOKEN_SECRET=
CORS_ORIGINS=http://localhost:4848,http://localhost:4849
