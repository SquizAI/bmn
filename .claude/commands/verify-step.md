# Verify Build Step

Run the verification commands for a specific build step to confirm it was completed correctly.

Read `docs/prd/BUILD-GUIDE.md`, find the specified step's "Verify:" section, and run each command. Report pass/fail status for each verification check.

Usage: /verify-step <step-number>
Example: /verify-step 1.2
