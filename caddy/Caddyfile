# Brand Me Now -- Caddy Reverse Proxy
# Automatic HTTPS via Let's Encrypt

# ─── Main Application ─────────────────────────────────────────────────
app.prznl.com {
    reverse_proxy client:80
}

api.prznl.com {
    reverse_proxy server:4847
}

prznl.com {
    redir https://app.prznl.com{uri} permanent
}

# ─── Influencer Storefronts (disabled until custom Caddy build with Cloudflare DNS plugin) ──
# Requires: caddy build with github.com/caddy-dns/cloudflare
# Re-enable when storefront container + custom Caddy image are deployed.
#
# *.brandmenow.store {
#     tls {
#         dns cloudflare {env.CLOUDFLARE_API_TOKEN}
#     }
#     @api path /api/*
#     handle @api {
#         reverse_proxy server:4847
#     }
#     handle {
#         reverse_proxy storefront:80
#     }
# }
#
# brandmenow.store {
#     redir https://app.prznl.com{uri} permanent
# }
