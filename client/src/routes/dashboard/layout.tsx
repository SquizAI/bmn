import { AppShell } from '@/components/layout/app-shell';

/**
 * Dashboard layout shell.
 * Wraps dashboard pages with header + sidebar.
 */
export default function DashboardLayout() {
  return <AppShell showSidebar />;
}
